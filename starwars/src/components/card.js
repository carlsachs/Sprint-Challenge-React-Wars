import React from "react";

const Card = props => {
    // eslint-disable-next-line no-unused-expressions
    <divWrap>
        <h2>Name: {props.data.name}</h2>
        <p>Height: {props.data.name}</p>
    </divWrap>
}
export default Card;